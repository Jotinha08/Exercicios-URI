#include <stdio.h>
#include <stdlib.h>
#include <string.h>
main(){

    int n, m, i, j;
    char a[30000]

    scanf("%d %d",&n &m);

    for(i=0; i<m; i++){
       scanf("%s",&a[i]);
      
    for(j=i; j<m; j++){
        if(a[j]==)
}
     
}
